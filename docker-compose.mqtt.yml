services:
  mqtt-broker:
    image: eclipse-mosquitto:2
    container_name: pandax-mqtt-broker
    ports:
      - "1883:1883"
    volumes:
      - ./mqtt/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
